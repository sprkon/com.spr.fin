openapi: 3.0.0
info:
  title: API Contract
  version: "1.0.0"
  description: "API specification (to be updated by /api-contract skill)"

servers:
  - url: http://localhost:3000/api
    description: Development server
  - url: https://api.example.com
    description: Production server

paths: {}

components:
  schemas: {}
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
